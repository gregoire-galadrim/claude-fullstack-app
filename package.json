{
  "name": "claude-fullstack-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/backend\" \"npm run dev -w packages/frontend\"",
    "build": "npm run build -w packages/frontend && npm run build -w packages/backend",
    "start": "NODE_ENV=production node packages/backend/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
